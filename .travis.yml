matrix:
    include:
    - os: osx
      script: ./build_and_deploy.sh

    - os: linux
      env: 
        - PYTHON_VER=3.6
      serivces: docker
      install: 
        - docker build -t atari-py-test -f dockerfile.${PYTHON_VER} . 
      script: 
        - docker run -e TWINE_USERNAME=$TWINE_USERNAME -e TWINE_PASSWORD=$TWINE_PASSWORD atari-py-test bash ./build_and_deploy.sh
    

        
